\documentclass[dvipdfmx,autodetect-engine, 12pt]{jsarticle}

\usepackage{ulem} % 取消線を引くためのパッケージ
\usepackage[dvipdfmx]{graphicx} % 画像を挿入するためのパッケージ
\usepackage{array} % 表の幅指定や自動改行に必要
\usepackage{enumitem}

% --- 参考文献用の設定 ---
\usepackage[backend=biber, style=numeric]{biblatex} % biblatexパッケージの使用
\addbibresource{../source.bib} % 先ほど作った別ファイルを指定（拡張子まで書く）
\usepackage{url} % WebサイトのURLを綺麗に出力するため

% --- section の文字サイズを小さくする設定 ---
\makeatletter
\renewcommand{\section}{%
  \@startsection{section}{1}{\z@}%
  {1.5ex \@plus .5ex \@minus .2ex}% 前の行との隙間
  {0.5ex \@plus .3ex}% 下の行との隙間
  {\normalfont\normalsize}% ← ここでサイズを \Large（少し大きめ）に指定
}
\makeatother

\begin{document}

% 課題のタイトル
\begin{center}

{\LARGE \bfseries 課題④\, ポートスキャンのパケット解析\par}

\end{center}

\vspace{1cm}

{\LARGE \uline{目的}} \\

\hangindent=1.5zw
\hangafter=0
\noindent
探査段階の中心となるポートスキャンの実習を行う．Nmap で探査中のパケットを調査する．パケットを分析することにより，探査の仕組みを確認する．

\vspace{1cm}

{\LARGE \uline{事前準備}} 

\begin{enumerate}
    \item server と router の VM を起動する．
    \item TCP の 3way handshake を調査する．
    \item ホスト検出，ポートスキャン手法について調査する．
    \item telnet と SSH の違いについて調査する．
\end{enumerate}

\vspace{1cm}

{\LARGE \uline{注意事項}}

\begin{enumerate}[label=・]
    \item 不用意にインターネット上のサーバに対して Nmap を利用してポートスキャンを行うと，スキャンを受けている側からすればアタックを受けているように見える．演習で構築した VM 以外のサーバ等に対してスキャンしないようにすること．
    \item 自分で構築，設定していない IP アドレスのマシンへの通信が勝手に発生している場合は，解析に含めなくてよい．
\end{enumerate}

\vspace{1cm}

{\LARGE \uline{手順}} 

\begin{enumerate}[label=(\arabic*),leftmargin=*]
    \item Nmap を使ったホスト検出 \\
    サブゴール：探査用マシン(server)が属するネットワーク内で動作しているすべてのマシンをリストアップする．
    \begin{enumerate}[label=\arabic*, leftmargin=*]
        
        \item server で Wireshark を起動する．
        
        \item 次のコマンドを実行して IP アドレスの指定範囲をスキャンすることで動作しているマシンを探し，IP アドレスを確認する．（Wireshark でパケットを記録すること） \\
        \texttt{\# nmap -sn スキャン対象の IP アドレスの範囲} \\
        （例えば \texttt{nmap -sn 172.20.1.1-254} あるいは \texttt{nmap -sn 172.20.1.0/24} アドレスの範囲はあくまで例です．どの範囲を指定すべきかは各自で考えること．）解析に時間がかかる場合には，範囲を限定すること． \\
        ※ ネットワーク内のすべてのマシンが検出されていることを確認すること． \\
        \mbox{}[解説]：個々のパケットではなく TCP ストリームを解析したい場合には，解析したい TCP ストリーム中のパケットを選択し，右クリックして表示される Follow TCP Stream を選択する．表示されるウィンドウ中の文字色の違いを把握しておくこと．

            \vspace{\baselineskip}
            \quad
            まずnmapについての説明を行う.
            nmapは, ネットワーク調査およびセキュリティ監査を行うためのオープンソースのツールである.
            生の(raw)IPパケットを用いて, ネットワーク上でどのようなホストか利用可能になっているか, 
            これらのホストが提供しているサービス(アプリケーション名とバージョン)は何か, 
            ホストが実行しているOS(OS名とバージョン)は何か, 
            どのような種類のパケットフィルタ/ファイアウォールが使用されているかなど,
            その他数多くの特徴を斬新な方法で判別する\cite{nmap_man_jp}.\\
            \quad
            ここでは-snオプションを用いる.
            このオプションを使うと, Nmapはpingスキャン(ホスト発見)のみを実行し, 
            応答した利用可能なホストの一覧を出力する.
            それ以上の調査(ポートスキャンやOS検出など)は行わない.
            pingスウィープやpingスキャンとも呼ばれるこの手法は, 
            デフォルトで、ICMPエコー要求と80番ポート宛てのTCPパケットを送信する. 
            高い権限を持つユーザが, ローカルイーサネットネットワーク上のターゲットのスキャンを試みる場合は,
            --send-ipが指定されていない限り、ARP要求(-PR)が用いられる\cite{nmap_man_host_discovery}.\\
            \quad
            pingスキャンのの実行結果を図\ref{fig:ping_scan_terminal}に示す.
            探索範囲には$192.168.100.1 \sim 192.168.100.5$を指定した.
            プライベートアドレス空間192.168.100.0/24に属するホストはrouterとserverのみであり,
            それぞれIPアドレスに192.168.100.1と192.168.100.2を割り当てている.
            したがって, これらのアドレスを含まないアドレス空間に対するpingスキャンは
            応答が期待できないためため, 上述のような探索範囲を指定した.

            \begin{figure}[htbp]
                \centering
                \includegraphics[width=0.8\textwidth]{imgs/1/nmap_ping_scan_result_terminal.png} 
                \caption{pingスキャンの結果}
                \label{fig:ping_scan_terminal}
            \end{figure}

            \quad
            図\ref{fig:ping_scan_terminal}の結果から, pingスキャンの結果として, 
            192.168.100.1と192.168.100.2の2つのIPアドレスが検出されたことがわかる.
            これはそれぞれrouterとserverのIPアドレスであり, 想定通りの結果が得られた.

            \vspace{\baselineskip}

        \item パケット解析した結果（パケットをキャプチャした結果など）を用いて，OSI 参照モデルのどの層で，どのような手順で，どのように，どのような情報が，やり取りされてホスト検出の仕組みが，実現されているかをレポートで報告しなさい．
        
            \vspace{\baselineskip}
            \quad
            pingスキャンを実行した際にwiresharkでキャプチャされたパケットの一覧と各パケットの詳細について図
            \ref{fig:nmap_ping_scan_result_arp_request}, \ref{fig:nmap_ping_scan_result_arp_reply}, \ref{fig:nmap_ping_scan_result_dns}, \ref{fig:nmap_ping_scan_result_icmp}に示す.

            \begin{figure}[htbp]
                \centering
                \includegraphics[width=0.8\textwidth]{imgs/1/nmap_ping_scan_result.png} 
                \caption{pingスキャン時にキャプチャされたパケット一覧}
                \label{fig:nmap_ping_scan_result}
            \end{figure}

            \quad
            図\ref{fig:nmap_ping_scan_result}はpingスキャンを実行した際にwiresharkでキャプチャされたパケットの一覧であり, 
            キャプチャ順にソートされている.
            ここから, pingスキャンの実行に伴い, ARPリクエスト・DNS・ICMPといったプロトコルのパケットがキャプチャされたことがわかる.
            最初にキャプチャされているパケットがARPによるものであり, 
            まずNo.1$\sim$4にのパケットにかけて特定のMACアドレスからブロードキャストで合計4つのパケットが送信されていることが分かる.
            infoフィールドにはWho has 192.168.100.[1,3,4,5]と記載されている.
            例として図\ref{fig:nmap_ping_scan_result}におけるNo.1のパケットの詳細を図\ref{fig:nmap_ping_scan_result_arp_request}に示す.
            
            \begin{figure}[htbp]
                \centering
                \includegraphics[width=0.8\textwidth]{imgs/1/nmap_ping_scan_result_arp_request.png} 
                \caption{ARPリクエスト}
                \label{fig:nmap_ping_scan_result_arp_request}
            \end{figure}

            \quad
            図\ref{fig:nmap_ping_scan_result_arp_request}では, 
            serverのIPアドレスである192.168.100.2から192.168.100.1のIPアドレスを解決するためのARPリクエストがブロードキャストされていることがわかる.

            \quad
            次にNo.5のパケットでユニキャストのパケットがキャプチャされていることが分かる.
            No.5のパケットの詳細を図\ref{fig:nmap_ping_scan_result_arp_reply}に示す.

            \begin{figure}[htbp]
                \centering
                \includegraphics[width=0.8\textwidth]{imgs/1/nmap_ping_scan_result_arp_reply.png} 
                \caption{ARP応答}
                \label{fig:nmap_ping_scan_result_arp_reply}
            \end{figure}

            \quad
            図\ref{fig:nmap_ping_scan_result_arp_reply}から, このパケットは
            192.168.100.1から192.168.100.2へのARP応答,
            すなわち, routerからserverへ返されたARP応答である.

            \quad
            続いてNo.6$\sim$8にかけて再びARP要求パケットがブロードキャストされていることが分かる.
            これは1回目のARP要求に対する応答が得られなかったIPアドレスに対して, 再度ARP要求がブロードキャストされたものであると考えられる.
            実際にキャプチャされたパケットにおいてもARP要求の再送が行われているのは192.168.100.[3,4,5]のIPアドレスに対してである.

            \quad
            次にキャプチャされたDNSのパケットの詳細について図\ref{fig:nmap_ping_scan_result_dns}に示す.

            \begin{figure}[htbp]
                \centering
                \includegraphics[width=0.8\textwidth]{imgs/1/dns.png} 
                \caption{DNS問い合わせ}
                \label{fig:nmap_ping_scan_result_dns}
            \end{figure}

            \quad
            図\ref{fig:nmap_ping_scan_result_dns}から, DNS$rightallow$Queriesにおいて,
            1.100.168.192.in-addr.arpa:type\,PTR, class\,INとの記述が読み取れる.
            DNS逆引きにおいてはドメイン名とタイプがそれぞれ,
             [逆順IP].IN-ADDR.ARPA, PTR
            になるため\cite{rfc1035},
            これはDNS逆引きのためのDNSパケットであることがわかる.
            今回は-nオプション(DNS解決を行わない)を指定していないため,
            DNS逆引きを試みている\cite{nmap_man_host_discovery}.


            \quad
            次に, ICMPパケットの詳細について図\ref{fig:nmap_ping_scan_result_icmp}に示す.

            \begin{figure}[htbp]
                \centering
                \includegraphics[width=0.8\textwidth]{imgs/1/nmap_ping_scan_result_icmp.png} 
                \caption{ICMP到達不能メッセージ}
                \label{fig:nmap_ping_scan_result_icmp}
            \end{figure}

            \quad
            図\ref{fig:nmap_ping_scan_result_icmp}から,
            ICMP $\rightarrow$ DNS $\rightarrow$ Transaction\,IDの値が図\ref{fig:nmap_ping_scan_result_dns}のDNS問い合わせのDNS $\rightarrow$ Transaction\,IDと同じであることから,
            このパケットは図\ref{fig:nmap_ping_scan_result_dns}のDNS問い合わせに対する応答であることがわかる.
            さらに, ICMPのTypeが3でCodeが3であることから,
            ポート到達不能によるICMP到達不能メッセージであることがわかる.

            \quad
            以上の解析から, pingスキャンの実行に伴い,
            まずARPリクエストが探索範囲に対して送信され,
            ARP応答が返されたIPのみが利用可能なホストとして検出されることがわかった.

            \vspace{\baselineskip}
    \end{enumerate}

    \vspace{0.5cm}
    \item Nmap を使ったポートスキャン \\
    サブゴール：探査用マシン(server)から，（１）ホスト検出で検出された各マシン（以下，被攻撃対象マシン）について順番に調査する
    \begin{enumerate}[label=\arabic*, leftmargin=*]
        \item telnet と ssh のポート番号以外の違いで最も大きな違いは何か調査しなさい．
        
            \vspace{\baselineskip}
            \quad
            telnetとsshはいずれもサーバ間のリモートアクセスに用いられるプロトコルであるが, sshでは通信内容が暗号化される一方でtelnetにおいては暗号化する仕組みがない\cite{telnet_ssh}.
            \vspace{\baselineskip}

        \item 3 種類のポートスキャンで，OSI 参照モデルのどの層で，どのような手順で，どのように，どのような情報が，やり取りされるか再確認し，整理しなさい．
        
            \vspace{\baselineskip}
            \quad
            3種類のポートスキャンとはTCP connectスキャン, SYNステルススキャン, UDPスキャンのことである.
            各ポートスキャンの詳細については以下の通りである.

        \begin{enumerate}
            \item TCP connectスキャン\\
                \quad
                この手法では他のほとんどのスキャンタイプと異なり, 
                生パケットに書き込むのではなく, connectシステムコールを発行して, 
                ターゲットのマシンのポートとコネクションを確立するよう下位OSに要求する. 
                前述のとおり, 高レベルのシステムコールを呼び出しており, 制御の自由度や処理効率が低く, 
                TCPコネクションを確立することからターゲットマシンのログにも残りやすい\cite{nmap_man_port_scanning_techniques}.

                \quad
                TCP\,connect\,スキャンの手順は以下のとおりである.

                \vspace{\baselineskip}

                \begin{description}[leftmargin=*]
                    \item[ターゲットのポートが開放(open)されている場合] \leavevmode
                        \begin{enumerate}[label=\textcircled{\scriptsize\arabic*}, leftmargin=*]
                            \item スキャン側から,ターゲットの対象ポートへ「接続したい」という要求（SYNパケット）を送信する.
                            \item ポートが開いているため, ターゲットから「接続を許可する」という応答（SYN/ACKパケット）が返ってくる.
                            \item スキャン側から,さらに「確認した」という応答（ACKパケット）を返す.
                        \end{enumerate}

                    \item[ターゲットのポートがポートが閉塞(closed)している場合] \leavevmode
                        \begin{enumerate}[label=\textcircled{\scriptsize\arabic*}, leftmargin=*]
                            \item スキャン側から,ターゲットの対象ポートへ「接続したい」という要求（SYNパケット）を送信する.
                            \item ポートが閉じているため, ターゲットから「接続を拒否する」という応答（RST/ACKパケット）が返ってくる.
                        \end{enumerate}
                \end{description}

                \vspace{\baselineskip}

                \quad
                図\ref{fig:tcp_connect_scan}は上述したTCP\,connect\,スキャンの手順をOSI参照モデルに基づいて図解したものである.

                \begin{figure}[htbp]
                    \centering
                    \includegraphics[width=0.8\textwidth]{imgs/2/tcp_scan_osi.png} 
                    \caption{OSIモデルに基づいたTCP Connectスキャンによるデータの流れ(\cite{yamaguchi_ain_week5}より引用・改変)}
                    \label{fig:tcp_connect_scan}
                \end{figure}

                \quad
                このスキャン手法はTCPコネクション確立の手続きを利用してポートのスキャンを行うため,
                基本的には, それぞれのやり取りにおいてトランスポート層が起点・終点となってポートスキャンが行われる.
                一方で, フィルタリングやファイアウォールの存在によって, 送信されたパケットがターゲットマシンのポートに到達しない場合もある.
                タイプ3, コード13 (宛先到達不能; 通信が管理上の理由で禁止されている)などのICMPエラーメッセージを返すこともあるが,
                応答しないで応答しないでパケットを破棄することが多い\cite{nmap_man_port_scanning}.
                このような場合はICMPヘッダや, 何も応答がないことを理由にフィルタされている(filtered)と判定される\cite{nmap_man_port_scanning_techniques}.
                したがって基本的には, TCP\,connect\,スキャンはTCPヘッダのフラグ情報をもとに,トランスポート層(第4層)以下で動作すると言える.
  
                \vspace{\baselineskip}
                
            \item SYNステルススキャン\\
                \quad
                nmapコマンドにおけるデフォルトのスキャン手法である. 
                ファイアウォールによる妨害がなく, TCPコネクションも確立しないため, 
                比較的秘匿性が高い. この手法ではTCPコネクションを確立するわけではないため, 
                ハーフオープンスキャンと呼ばれることも多い\cite{nmap_man_port_scanning_techniques}.

                \quad
                SYNステルススキャンの手順は以下のとおりである.

                \vspace{\baselineskip}
                \begin{description}[leftmargin=*]
                    \item[ターゲットのポートが開放(open)されている場合] \leavevmode
                        \begin{enumerate}[label=\textcircled{\scriptsize\arabic*}, leftmargin=*]
                            \item スキャン側から,ターゲットの対象ポートへ「接続したい」という要求（SYNパケット）を送信する.
                            \item ポートが開いているため, ターゲットから「接続を許可する」という応答（SYN/ACKパケット）が返ってくる.
                            \item スキャン側から,さらに「確認した」という応答（RSTパケット）を返す.
                        \end{enumerate}

                    \item[ターゲットのポートがポートが閉塞(closed)している場合] \leavevmode
                        \begin{enumerate}[label=\textcircled{\scriptsize\arabic*}, leftmargin=*]
                            \item スキャン側から,ターゲットの対象ポートへ「接続したい」という要求（SYNパケット）を送信する.
                            \item ポートが閉じているため, ターゲットから「接続を拒否する」という応答（RST/ACKパケット）が返ってくる.
                        \end{enumerate}
                \end{description}
                \vspace{\baselineskip}
                
                \quad
                図\ref{fig:syn_scan}は上述したSYNステルススキャンの手順をOSI参照モデルに基づいて図解したものである.

                \begin{figure}[htbp]
                    \centering
                    \includegraphics[width=0.8\textwidth]{imgs/2/syn_scan_osi.png} 
                    \caption{OSIモデルに基づいたSYNステルススキャンによるデータの流れ(\cite{yamaguchi_ain_week5}より引用・改変)}
                    \label{fig:syn_scan}
                \end{figure}

                \quad
                このスキャン手法もTCPコネクション確立の手続きを利用してポートのスキャンを行うため,
                基本的には, それぞれのやり取りにおいてトランスポート層が起点・終点となってポートスキャンが行われる.
                TCP\,connect\,スキャンと異なるのは, 
                TCP\,connect\,スキャンと同じく, 一部例外を除けば基本的には図\ref{fig:syn_scan}の通り
                トランスポート層を起点・終点としてポートスキャンが実行されるため, 
                SYNステルススキャンはTCPヘッダのフラグ情報をもとに,
                トランスポート層(第4層)以下で動作すると言える.
                
                \vspace{\baselineskip}
            \item UDPスキャン\\
            
                空の(データなし)UDPヘッダを各ターゲットポートに送ることで機能する.
                このスキャン手法の問題は, 処理が低速な点である.
                ほか２つのスキャン手法と異なり, 
                ポートが開いている場合にはターゲットからの応答がないため,
                パケットや応答が行方不明になった場合に備えて再試行を行うことになる.
                また, ポートが閉じている場合にはICMPポート到達不能エラーが帰ってくるが,
                ICMP到達不能メッセージはデフォルトでレート制限されているため, 
                すべてのポートをスキャンするのに膨大な時間を要するという問題がある\cite{nmap_man_port_scanning_techniques}.

                \quad
                UDPスキャンの手順は以下のとおりである.

                \vspace{\baselineskip}
                \begin{description}[leftmargin=*]
                    \item[ターゲットのポートが開放(open)されている場合] \leavevmode
                        \begin{enumerate}[label=\textcircled{\scriptsize\arabic*}, leftmargin=*]
                            \item スキャン側から, ターゲットの対象ポートへ空のUDPヘッダ（データなしパケット）を送信する.
                        \end{enumerate}
                    
                    \item[ターゲットのポートが閉塞(closed)している場合] \leavevmode
                        \begin{enumerate}[label=\textcircled{\scriptsize\arabic*}, leftmargin=*]
                            \item スキャン側から, ターゲットの対象ポートへ空のUDPヘッダ（データなしパケット）を送信する.
                            \item ポートが閉じているため, ターゲットのOS（ネットワーク層）から「ポートに到達できない」という応答（ICMPポート到達不能エラー：Type 3, Code 3）が返ってくる.
                        \end{enumerate}
                \end{description}
                \vspace{\baselineskip}

                \quad
                図\ref{fig:udp_scan}は上述したUDPスキャンの手順をOSI参照モデルに基づいて図解したものである.

                \begin{figure}[htbp]
                    \centering
                    \includegraphics[width=0.8\textwidth]{imgs/2/udp_scan_osi.png} 
                    \caption{OSIモデルに基づいたUDPスキャンによるデータの流れ(\cite{yamaguchi_ain_week5}より引用・改変)}
                    \label{fig:udp_scan}
                \end{figure}

                \quad
                空の(データなし)UDPヘッダを各ターゲットポートに送ることで機能する.
                基本的には, ICMPポート到達不能エラー(タイプ3, コード1,2,9,10,13)が返された場合,
                ポートは閉じており(closed), その他のICMPポート到達不能エラーがが返された場合,
                ポートはフィルタされている(filtered).
                まれにサービスがUDPパケットで応答する場合もあるが, その場合はポートは開いている(open)ことがわかる.
                数回の再試行の後も応答がない場合にはポートは開いているあるいはフィルタされている(open|filtered)と判別することができる\cite{nmap_man_port_scanning_techniques}.
                UDPスキャン手法もトランスポート層(第4層)以下で動くが,
                ほか2つの手法と異なり, この手法は基本的にポートの状態の判断は,
                ICMPエラーメッセージの有無(ネットワーク層)で行われる.


            \end{enumerate}
            \vspace{\baselineskip}
        
        \item ［被攻撃対象マシン］で telnetd と dropbear と ftp が起動していないことを確認する．起動している場合には停止させる．起動しているかどうかは ps コマンドなどで確認すること． \\
        ※ ftp は ftp という文字を含むプログラムが動いていないことを確認
        
            \vspace{\baselineskip}
            \quad
            図\ref{fig:router_process}はrouterにおいて,
            psコマンドとgrepコマンドを組み合わせて
            telnetdとdropbear, ftpのプロセスが起動していないことを確認するコマンドを実行した結果である.

            \begin{figure}[htbp]
                \centering
                \includegraphics[width=0.8\textwidth]{imgs/2/no_process.png} 
                \caption{routerにおける起動プロセスの確認}
                \label{fig:router_process}
            \end{figure}

            grepによる条件を満たすプロセス(telnetまたはdropbearまたはftpという文字列を含むプロセス)は,
            ここで実行したgrepコマンドによるもののみであるため, 
            telnet, dropbear, ftp いずれのプロセスも起動していないことが確認できる.

            \vspace{\baselineskip}
        
        
        \item ［探査用マシン］で Wireshark を起動し，以下の 4 つのポートスキャンによって生じた双方向の通信の内容を解析しなさい． \\
        ※ 先に以降の手順の意図を把握し，どのような結果を得るべきか考えて，解析をすることを奨める．また，適宜，解析を区切るなど，工夫して実施してもよい． \\
        ※ 結果の見通しをよくするためスキャン範囲を telnetd と dropbear が使用するポート番号の前後に絞ってもよいが、関係ないポートも一部含めること

        telnet, dropbearが使用するポートはそれぞれ23,22である\cite{infraexpert_telnet_ssh}\cite{ubuntu_dropbear_man}.
        ここでは, 非攻撃対象マシンであるrouterに対して
        telnetとdropbearのポートとftpの利用するポートである21と24を含めた
        21から24の範囲にポートスキャンを行った.

        \begin{enumerate}[label=・]
            \item TCP connect スキャンを行う．
                図\ref{fig:tcp_conncet_scan_result_after}はTCP\,connect\,スキャンの実行結果である. 
                \begin{figure}[htbp]
                    \centering
                    \includegraphics[width=0.8\textwidth]{imgs/2/no_process_st.png} 
                    \caption{TCP\,connect\,スキャンの実行結果(サービスダウン時)}
                    \label{fig:tcp_connect_scan_result_after}
                \end{figure}

                \quad
                ターミナルにおける実行結果から, 探索範囲にしたすべてのポートの状態(STATE)がclosedと表記されているため,
                routerにおける21から24のすべてのポートが閉じていることが確認できる.\\
                \quad
                また, wiresharkでのキャプチャ結果から,
                serverからrouterに対して, SYNパケットが送信され,
                routerからserverに対して, RST/ACKパケットが返されていることがわかる.
                これは, 図\ref{fig:tcp_scan_osi}におけるTCP\,connect\,スキャンの手順において, 
                ターゲットのTCPポートが閉塞(closed)している場合の手順と一致している. 
                \vspace{\baselineskip}

            \item SYN ステルススキャンを行う．\\
                図\ref{fig:syn_scan_result_before}
                はSYNステルススキャンの実行結果である. 
                \begin{figure}[htbp]
                    \centering
                    \includegraphics[width=0.8\textwidth]{imgs/2/no_process_ss.png} 
                    \caption{SYNステルススキャンの実行結果(サービスダウン時)}
                    \label{fig:syn_scan_result_before}
                \end{figure}

                \quad
                ここから, 探索範囲にしたすべてのポートの状態(STATE)がclosedと表記されているため,
                routerにおける21から24のすべてのポートが閉じていることが確認できる.\\

                \quad
                また, wiresharkでのキャプチャ結果から,
                serverからrouterに対して, SYNパケットが送信され,
                routerからserverに対して, RST/ACKパケットが返されていることがわかる.
                これは, 図\ref{fig:syn_scan_osi}におけるSYNステルススキャンの手順において, 
                ターゲットのTCPポートが閉塞(closed)している場合の手順と一致している. 
                \vspace{\baselineskip}


                
            \item UDP スキャンを行う．
            
                図\ref{fig:udp_scan_result_before}はUDPスキャンの実行結果である. 
                \begin{figure}[htbp]
                    \centering
                    \includegraphics[width=0.8\textwidth]{imgs/2/no_process_su.png} 
                    \caption{UDPスキャンの実行結果(サービスダウン時)}
                    \label{fig:udp_scan_result_before}
                \end{figure}

                \quad
                ターミナルでの実行結果から, 
                探索範囲にしたすべてのポートの状態(STATE)がclosedと表記されているため,
                routerにおける21から24のすべてのUDPポートが閉じていることが確認できる.\\
                \quad
                また, wiresharkでのキャプチャ結果から,
                serverからrouterに対して, 
                Info$\rightarrow$Lenが0すなわち, 
                空データのUDPパケットがserverからrouterに送信され,
                routerからserverに対してICMPのポート到達不能メッセージが送信されていることが確認できる.
                これは図\ref{fig:udp_scan_osi}
                におけるUDPスキャンの手順においてターゲットのポートが
                閉鎖(closed)している場合の手順と一致している.
                \vspace{\baselineskip}

            \item TCP/IP fingerprinting を用いて OS を判別する．
            
                \vspace{\baselineskip} 
                \quad   
                図\ref{fig:fingerprint_scan_result_before}はTCP/IP\, fingerprinting を用いて OS を判別の実行結果である.
                \begin{figure}[htbp]
                    \centering
                    \includegraphics[width=0.8\textwidth]{imgs/2/no_process_o.png} 
                    \caption{TCP/IP fingerprinting を用いて OS を判別の実行結果(サービスダウン時)}
                    \label{fig:fingerprint_scan_result_before}
                \end{figure}


                \quad
                ターミナルの実行結果から,
                探索範囲にしたすべてのポートの状態(STATE)がclosedと表記されているため,
                routerにおける21から24のすべてのポートが閉じていることが確認できる.\\
                ここから, -Oオプションによるos判別のコマンドが実行されると,
                大量のTCP, UDP, ICMPパケットが行き交っていることがわかる.
                nmapにおけるOS検出ではTCP,UDP,ICMPの様々な調査用パケットをターゲットに対して送信し\cite{nmap_os_detection},
                その応答を調査することによってターゲットのOSを判別するため\cite{nmap_man_osdetect_ja},
                ここでキャプチャされた大量のパケットそれらの調査パケットあるいはその応答パケットであると考えられる.
                \vspace{\baselineskip}

        \end{enumerate}
        \item ［被攻撃対象マシン］で telnetd と dropbear と ftp を起動させる．dropbear を起動させる際には，オプションとして -R をつけること．FTP server は，既にインストールされているソフト(GUI)を起動させればよい．
        \item telnetd と dropbear と ftp が起動していることを確認する．
        
            \vspace{\baselineskip}

            \quad
            図\ref{fig:active_check}はrouter[被攻撃対象マシン]においてtelnetdとdropbearとftpが起動していることを確認した結果である.
            \begin{figure}[htbp]
                \centering
                \includegraphics[width=0.8\textwidth]{imgs/2/active_check.png} 
                \caption{telnetdとdropbearとftpが起動していることの確認結果}
                \label{fig:active_check}
            \end{figure}

            \quad
            grepによる条件を満たすプロセス(telnetまたはdropbearまたはftpという文字列を含むプロセス)がgrepによるプロセス以外に,
            telnetとdropbearとftpに対応するものがそれぞれ1つずつ確認できることから, telnetdとdropbearとftpが起動していることが確認できる.
            \vspace{\baselineskip}

        \item ［探査用マシン］から［被攻撃対象マシン］へ telnet および ssh で作成した一般権限ユーザ名でログインできることを確認する．
        
            \vspace{\baselineskip}
            \quad
            図\ref{fig:login_check}はserverからrouterへtelnetとsshでログインを試みた結果である.
            \begin{figure}[htbp]
                \centering
                \includegraphics[width=0.8\textwidth]{imgs/2/telnet_ssh_login_success.png} 
                \caption{serverからrouterへtelnetとsshでログインを試みた結果}
                \label{fig:ssh_login_check}
            \end{figure}

            \quad
            ここから, telnetとsshの両方でserverからrouterにログインに可能であることが確認できる.
            \vspace{\baselineskip}
            
        \item ［探査用マシン］から［被攻撃対象マシン］へ telnet および ssh でログインする過程で生じた通信の内容を解析し，調査結果より想定される telnet と ssh の最も大きな違いが確認できることを，実験した結果（比較を含む）を用いて，レポートで報告しなさい． \\
        ヒント：TCP stream でログイン時のパケット内の情報について確認する

            \vspace{\baselineskip}
            \quad
            図\ref{fig:telnet_login_packets}, \ref{fig:ssh_login_packets}は
            探査用マシンから被攻撃対象マシンへtelnetおよびsshでログインする過程でwiresharkでキャプチャされたパケットの一覧である.
            \begin{figure}[htbp]
                \centering
                \includegraphics[width=0.8\textwidth]{imgs/2/telnet_w.png} 
                \caption{telnetでログインにキャプチャされたパケットの一覧}
                \label{fig:telnet_login_packets}
            \end{figure}
            \begin{figure}[htbp]
                \centering
                \includegraphics[width=0.8\textwidth]{imgs/2/ssh_w.png} 
                \caption{sshでログイン時にキャプチャされたパケットの一覧}
                \label{fig:ssh_login_packets}
            \end{figure}

            \quad
            telnetでログインシた際にキャプチャされたパケットの一覧を確認すると,
            最初に, 3wayハンドシェイクによりserverとrouterとの間で通信が確立された後に,
            TCP及びTELNETプロトコルの大量のパケットがキャプチャされていることがわかる.\\
            \quad 
            一方で, sshでログインした際にキャプチャ刺されたパケットでは,
            同じく3wayハンドシェイクによりserverとrouterとの間で通信が確立された後に,
            TCP及びSSHv2プロトコルの大量のパケットがキャプチャされていることがわかる.\\
            \quad
            図\ref{fig:telnet_login_tcp_stream}, \ref{fig:ssh_login_tcp_stream}に
            図\ref{fig:telnet_login_packets}, \ref{fig:ssh_login_packets}で
            得られたパケットにおけるTCPストリームの例を示す.

            \begin{figure}[htbp]
                \centering
                \includegraphics[width=0.8\textwidth]{imgs/2/telnet_w_tcp_stream.png} 
                \caption{telnetでログイン時に得られたパケットの例}
                \label{fig:telnet_login_tcp_stream}
            \end{figure}
            \begin{figure}[htbp]
                \centering
                \includegraphics[width=0.8\textwidth]{imgs/2/ssh_w_tcp_stream.png} 
                \caption{sshでログイン時に得られたパケットの例}
                \label{fig:ssh_login_tcp_stream}
            \end{figure}

            \quad
            TCPストリームの内容を比較すると,
            telnetによる通信によって生じたパケットにおいては
            ユーザ名とパスワードがそのままTCPストリームに埋め込まれているのに対して,
            sshによる通信によって生じたパケットでは, 
            通信の内容が暗号化されていることが確認できる.

            \vspace{\baselineskip}


        \item 再び，手順 4.を実行し，以前の手順 4.での結果（出力とキャプチャパケットの面で）と比較し，整理した各ポートスキャンの動作の仕組み（OSI 参照モデルのどの層で，どのような手順で，どのように，どのような情報が，やり取りされるか）を，実験した結果（比較を含む）を用いて，レポートで報告しなさい．
        
            \begin{enumerate}[label=・]
            \item TCP connect スキャンを行う．\\
                \vspace{\baselineskip}
                \quad
                図\ref{fig:tcp_conncet_scan_result_after}はTCP\,connect\,スキャンの実行結果である. 
                \begin{figure}[htbp]
                    \centering
                    \includegraphics[width=0.8\textwidth]{imgs/2/active_nmap_sT.png} 
                    \caption{TCP\,connect\,スキャンの実行結果(サービス起動時)}
                    \label{fig:tcp_connect_scan_result_after}
                \end{figure}

                \quad
                ターミナルでの実行結果から, 
                tcpのポート21$\sim$23, サービス名にするとftp,ssh,telnetのポートが開放(open)されており,
                tcpのポート24は閉鎖(closed)されていることが確認できる.\\
                \quad
                続いて, wiresharkでのキャプチャ結果を確認する.
                routerの21$\sim$23のtcpポートとのやり取りに着目すると,
                serverからrouterに対して, SYNパケットが送信され,
                routerからserverに対して, SYN/ACKパケットが返されたのち,
                serverからrouterに対して, ACKパケットが送信されていることがわかる.
                これは, 図\ref{fig:tcp_scan_osi}におけるTCP\,connect\,スキャンの手順において, 
                ターゲットのTCPポートが開放(open)されている場合の手順と一致している.
                tcpポート24とのやり取りに関しては,
                手順4において得られた結果と同様である.

                % 図\ref{fig:tcp_connect_scan_result_w_2}は図\ref{fig:tcp_conncet_scan_result_2}においてスキャンが行われた際に
                % キャプチャされたパケットの一覧を示したものである.
                % \begin{figure}[htbp]
                %     \centering
                %     \includegraphics[width=0.8\textwidth]{imgs/2/active_nmap_sT_w.png} 
                %     \caption{TCP\,connect\,スキャンの実行時にキャプチャされたパケット一覧2}
                %     \label{fig:tcp_connect_scan_result_w_2}
                % \end{figure}
                
                \vspace{\baselineskip}



            \item SYN ステルススキャンを行う．
                図
                \ref{fig:tcp_conncet_scan_result_after}
                はSYNステルススキャンの実行結果である. 
                \begin{figure}[htbp]
                    \centering
                    \includegraphics[width=0.8\textwidth]{imgs/2/active_nmap_sS.png} 
                    \caption{SYNステルススキャンの実行結果2}
                    \label{fig:syn_scan_result_after}
                \end{figure}

                \quad
                ターミナルでの実行結果から, 
                tcpのポート21$\sim$23, サービス名にするとftp,ssh,telnetのポートが開放(open)されており,
                tcpのポート24は閉鎖(closed)されていることが確認できる.\\
                \quad
                続いて, wiresharkでのキャプチャ結果を確認する.
                routerの21$\sim$23のtcpポートとのやり取りに着目すると,
                serverからrouterに対して, SYNパケットが送信され,
                routerからserverに対して, SYN/ACKパケットが返されたのち,
                serverからrouterに対して, RSTパケットが送信されていることがわかる.
                これは, 図\ref{fig:syn_scan}におけるSYNステルススキャンの手順において, 
                ターゲットのTCPポートが開放(open)されている場合の手順と一致している.
                tcpポート24とのやり取りに関しては,
                手順4において得られた結果と同様である.

                \vspace{\baselineskip}
                
            \item UDP スキャンを行う．
            
                図\ref{fig:udp_scan_result_after}はUDPスキャンの実行結果である. 
                \begin{figure}[htbp]
                    \centering
                    \includegraphics[width=0.8\textwidth]{imgs/2/active_nmap_sU.png} 
                    \caption{UDPスキャンの実行結果}
                    \label{fig:udp_scan_result_after}
                \end{figure}

                \quad
                ターミナルでの実行結果から, 
                探索範囲内のすべてのudpポートが閉鎖(closed)されていることが確認できる.\\
                \quad
                続いて, wiresharkでのキャプチャ結果では, 
                データ長0, すなわち, 空のUDPパケットがserverからrouterへ送られた後に,
                routerからserverに対してICMPのポート到達不能メッセージが送信されていることが確認できる.
                図\ref{fig:udp_scan_osi}におけるUDPスキャンの手順において, 
                ターゲットのポートが開放(open)されている場合の手順と一致していることが分かる.


                \vspace{\baselineskip}
            \item TCP/IP fingerprinting を用いて OS を判別する．
            
                \quad
                図\ref{fig:fingerprint_scan_result_after}はTCP/IP\, fingerprinting を用いて OS を判別の実行結果である.
                \begin{figure}[htbp]
                    \centering
                    \includegraphics[width=0.8\textwidth]{imgs/2/active_nmap_O.png} 
                    \caption{TCP/IP fingerprinting を用いて OS を判別の実行結果2}
                    \label{fig:fingerprint_scan_result_after}
                \end{figure}   

                \quad
                ターミナルの実行結果から,



        \end{enumerate}
            
            
        \item 手順 8.の比較の内，TCP/IP fingerprinting を用いた OS 判別において，3 つのポートスキャン方法の内，どの方法が利用されているか，実験した結果（比較を含む）を用いて，レポートで報告しなさい．また，OS の検出結果について違いが起きたか報告しなさい．
    

    \end{enumerate}
\end{enumerate}


\newpage

% --- 参考文献リストの出力 ---
\printbibliography[title=参考文献]

\end{document}